# Codex 项目导航

- 必读：本文件、[00_项目定位与边界](./项目说明_1.1/00_项目定位与边界.md)、[06_当前版本任务](./项目说明_1.1/06_当前版本任务.md)。`项目说明_1.1/` 是唯一内部权威说明，保留 00–07 与模块结构；软件版本为 4.4，Target Profile JSON 格式仍为 1.0。
- 分层加读：架构/目录/依赖任务读 01、02、04、07；公共接口/数据契约任务读 03 及受影响模块说明；路线优先级任务读 05；具体模块任务只读对应模块说明，不默认读取全部文档。
- 本 AGENTS 所在 Git 根是唯一现行源码，软件版本 4.4。外层文件夹名称不代表软件版本；项目内不得保存完整旧版源码、嵌套 Git 工作树、源码 ZIP 或 `.tmp_*source*`。备份放项目外，不创建 `versions/`、`src/`、`app/` 外壳。
- README 是给人的当前介绍，不是新需求；THIRD_PARTY_NOTICES 与版权声明不是架构需求。运行结果、研究计划、PPT、论文、实验方案及审计历史不得自动转成软件需求。
- 不因“未来/预留”接口自行实现功能；当前不实现 Agent、不建空框架，底层不得依赖它。
- 修改前检查现有接口与目录职责，优先扩展已有契约，不建平行架构；任务与架构原则冲突时先报告，不自行绕过。
- 开发后运行相关且不覆盖真实项目数据的测试，至少核对 04/05/06/07；公共契约、目录职责、模块 API 或用户可见能力变化时，同步真正受影响的 03/02/模块文档/README，不要求每次修改所有说明。
- 默认只做本地修改；发布须由用户另行明确要求。本轮用户已明确授权命名 Amadeus_demo_4.4 并上传 GitHub；不创建 PR，不将本轮授权延续为未来自动发布。
- `.gitignore` 对明确分类内的单层 `*测试.py` 放行，conftest、benchmark 工具和输入 fixture 精确白名单；不使用 `!测试/**/*.py`，不放行嵌套生成源码。新增测试先核对目录和白名单。数据、审计/运行/模型产物、缓存、数据库、日志、.env、凭据及生成的 Python 副本不属于公开源码。
- 首轮源码只读目标文件和直接依赖，遵循 06 的读取范围；缺信息时每次最多扩展 5 个直接相关文件并说明原因。禁止默认全仓库 tree/find/rg --files，不扫描 git 历史。
- 仅解析直接依赖时使用限定目录、限定符号的 rg；数据、缓存、运行输出、论文原件不得用于熟悉项目。
- 修改范围必须与当前任务一致。
- `.ignore` 定义搜索边界：默认不搜索数据、审计、运行、模型、缓存、依赖环境及压缩包；必要 fixture 仅按测试声明的精确路径读取。忽略规则不能代替正确输出路径。
- 清理先核对引用、实际行为和 07 的保留清单；抽象接口、未启用插件、注释设置、包初始化文件和 .gitkeep 不因空白或未启用被删除。一次性交付脚本不作为扩展点，删除须确认其用途已结束。

- 00–05、07 维护当前事实；06 是唯一当前任务文档，下轮直接替换。模块说明维护正式契约；失效说明直接替换/删除，不追加版本历史附录，文档不得领先实现声称已完成。
- 功能插件 canonical ID 来自自身插件标识，alias 显式，重复拒绝；替换必须显式并校验 alias。专用能力保留其 Protocol/ABC，不进入 God Context。
- 正式 run 共用现有运行管理器，写数据/运行记录/run_...；真正临时使用系统 TEMP/TMP 清理，不在根建时间戳目录。明确调试保留的 .cache/temp/ 不作数据总线；原子文件保存允许同目录短期临时文件并及时清理。
- pytest 只用 tmp_path/系统 TEMP，禁止将 `--basetemp` 指向项目或其祖先；默认离线并保护真实项目文件。benchmark 通过 `测试/benchmark工具/benchmark_artifacts.py` 校验写入 `审计结果/benchmark/<run_id>/`，冻结结果、审计、正式运行和模型默认保留，软件启动不得自动删除。历史根运行记录归入 `审计结果/legacy/`，不再作为写入入口。
- 修改后先跑受影响测试，再默认 pytest、compileall 和轻量仓库卫生回归；区分已知失败、新增失败与缺失 fixture，不删除科学断言或复制旧输出来制造通过。
- 旧 import shim 只导出 canonical 实现；内部生产代码不得依赖旧 shim。用途功能插件实现在 `插件/颅骨透明化/颅骨透明化功能插件.py`；历史运行器属于核心编排。UI 使用公开 `解析物性配置`，不导入 `_配置对象`。插件 ID、alias 与输入/输出契约兼容。

- UI 只调用核心公开入口，不直接执行具体插件或访问 controller.plugin_manager / `.插件管理器`；用途元数据与算法、通用规则引擎与默认预设分别维护。新能力优先专用接口，不扩 God Context；配方研发流程只编排，不承载新增模型实现，不预建空接口。

- 默认具体实现选择集中在 `核心系统/默认装配.py`；核心工作流依赖获取器与专用接口，不重新分散具体插件构造。装配层不执行业务、不读取用户数据、不联网、不保存运行状态。

- 普通开发/测试任务默认不读取 `测试/benchmark工具/`、`审计结果/` 和 benchmark 大型数据；仅明确 benchmark/科学验证任务按精确路径读取。测试科学输入使用 `测试/fixtures/seethrough/` 的最小离线记录，不读取个人完整缓存或历史结果。

- 第三方算法优先 adapter，不 vendor 或重写成熟算法；computed / predicted / measured 严格区分，预测结果保留 model/version/applicability/provenance。
- 可选重依赖不得进入默认运行路径，不自动联网、装依赖、下载模型或建立索引。
- 普通物质筛选始终先于未来下游色素/光谱增强；色素候选不能污染普通初筛或默认配方。不因未来色素需求修改当前普通 RI 算法。
